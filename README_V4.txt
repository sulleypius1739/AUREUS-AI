AUREUS V4 — A+ MARKET MECHANICS

This build replaces the previous multi-filter V3 strategy with ONE core model.

Run:
  python -m backtest.runner

Then enter:
  data\EURUSD_m5.csv

Web:
  node server.js
  http://localhost:8000

President/admin and live-dashboard files are retained from the previous web build.

See V4_MODEL_RULES.md for the exact mechanical setup.
