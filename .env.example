# Optional: use fixed first-run President credentials.
ADMIN_EMAIL=president@aureus.local
ADMIN_PASSWORD=change-this-to-a-long-random-password
PORT=8000
# Set true only when the site is actually served over HTTPS.
AUREUS_HTTPS=false
